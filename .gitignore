# data is reproducible from the HN API, no reason to commit it
data/

# my own notes, not part of the repo
.notes.md

# python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# secrets
.env
.env.*
!.env.example

# terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example

# node / next
node_modules/
.next/
out/

# macos
.DS_Store

# working checklist, has internal framing in it
PLAN.md

# tally output, mine not the repos
docs/format_tally.json

# pipeline outputs, rebuildable
data/postings.parquet
data/needs_llm.json

# pipeline output when CI points HN_RAW_DIR at the fixtures
tests/postings.parquet
tests/needs_llm.json

# eval run log, regenerated
evals/runs.jsonl

# drawn sample, regenerate with evals/sample.py
evals/sample.json

# hand labels and the drawn sample — mine, and regenerable
evals/labeled.jsonl
